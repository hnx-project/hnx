[package]
name = "loader-service"
version = "0.1.0"
edition = "2021"

[dependencies]
hnx-abi = { path = "../../../abi-bindings" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
